<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/coffeeList">

    <fragment
        android:id="@+id/coffeeList"
        android:name="com.example.coffeeapp.CoffeeList"
        android:label="fragment_coffee_list"
         >
        <action
            android:id="@+id/action_coffeeList_to_blankFragment"
            app:destination="@id/blankFragment"
            app:popUpToInclusive="false" />
    </fragment>
    <fragment
        android:id="@+id/blankFragment"
        android:name="com.example.coffeeapp.BlankFragment"
        android:label="fragment_blank"
        tools:layout="@layout/fragment_blank" />
</navigation>